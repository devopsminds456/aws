# IAM Roles & Policies

## Overview
Use IAM roles instead of static credentials to grant least-privilege access.

## Best Practices
- Assign roles to EC2 instances via Instance Profiles.
- Use managed policies where possible.
- Follow principle of least privilege.

## Risks
- Overly permissive policies can expose sensitive resources.

